// Indices — spectral / terrain indices alias.
// Re-exports raster terrain presets under the historical name so older
// Conservation panel imports (`indices.ts` from geospax-raster.js era) stay superseded.

export * from "./raster";
export * from "./terrain";
